package mobile.groupone.bookmanagergroupone;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.widget.EditText;

public class SummaryActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //setContentView(R.layout.activity_summary);

        //SimpleBookManager library = new SimpleBookManager();

        //EditText editText1 = (EditText) findViewById(R.id.number);
        //editText1.setText(Integer.toString(library.count()));

        //EditText editText2 = (EditText) findViewById(R.id.totCost);
        //editText2.setText(library.getTotalCost() + " SEK");

        //EditText editText3= (EditText) findViewById(R.id.mostExp);
        //editText3.setText(library.getMaxPrice() + " SEK");

        //EditText editText4 = (EditText) findViewById(R.id.lestExp);
        //editText4.setText(library.getMinPrice() + " SEK");

        //EditText editText5 = (EditText) findViewById(R.id.avPrice);
        //editText5.setText((Float.toString(library.getMeanPrice()) + " SEK"));
    }
}
