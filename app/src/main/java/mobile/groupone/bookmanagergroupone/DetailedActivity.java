package mobile.groupone.bookmanagergroupone;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.widget.EditText;

public class DetailedActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //setContentView(R.layout.activity_detailed);

        //SimpleBookManager library = new SimpleBookManager();
        //Book bookOne = library.getBook(0);

        //EditText editText1 = (EditText) findViewById(R.id.title);
        //editText1.setText(bookOne.getTitle());

        //EditText editText2 = (EditText) findViewById(R.id.author);
        //editText2.setText(bookOne.getAuthor());

        //EditText editText3= (EditText) findViewById(R.id.price);
        //editText3.setText(bookOne.getPrice() + " SEK");

        //EditText editText4 = (EditText) findViewById(R.id.isbn);
        //editText4.setText(bookOne.getIsbn());

        //EditText editText5 = (EditText) findViewById(R.id.course);
        //editText5.setText(bookOne.getCourse());
    }
}
